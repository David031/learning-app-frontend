query User($where: UserWhereUniqueInput!){
    user(where:$where){
        name
        email
        maxUnlockedLevel{
            code
        }
        records{
            id
            level{
                code
            }
        }
    }
}